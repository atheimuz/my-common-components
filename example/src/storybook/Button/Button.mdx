import { Story, Preview } from "@storybook/addon-docs/blocks";

# Button

##### Button 컴포넌트에 대한 설명입니다.

#

## props

| props       |         설명         |                                                                  값 |                  기본값 |       type |
| ----------- | :------------------: | ------------------------------------------------------------------: | ----------------------: | ---------: |
| `className` |       class명        | `theme: theme1 ~ theme10, clear, disabled \ size: size-xs ~ size-l` | `rs-button + className` |   `string` |
| `style`     |        스타일        |                                                          `(custom)` |                    none |   `object` |
| `disabled`  |       비활성화       |                                                       `true, false` |                   false |  `boolean` |
| `onClick`   | 클릭시 실행되는 함수 |                                                          `(custom)` |                    none | `function` |
| `children`  |    버튼 안의 내용    |                                                          `(custom)` |                    none |  `element` |

## element

<Preview>
    <Story id="button--style" />
</Preview>

## theme

<Preview>
    <Story id="button--theme" />
</Preview>